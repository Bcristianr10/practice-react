import { useState,useEffect } from "react"
const Component =() =>{
    const [value, setValue] = useEffect(false)

    // useEffect(codeToExecute, listOfDependecies)
    useEffect(()=>{
        // Se ejecuta como minimo una vez
    }, 
    // Es un Array, Opcinal y si no se pasa se ejecuta cada vez que se renderice.
    listOfDependecies
    )

}